node_modules
.next
.next-e2e
out
build
coverage
playwright-report
test-results
.env
.env.local
*.tsbuildinfo
next-env.d.ts
projects/*
!projects/.gitkeep

# Local run logs / scratch output
contend.txt
live-e2e-output.txt
*.log

# Local drafts, not published
Discord Intro.md

# Working notes that live alongside the published docs but are not part of them.
# Named rather than `docs/*/` so a future subfolder meant for the repo is not
# swallowed silently — the rest of docs/ is tracked.
docs/build-specs/
docs/quality-assessment/

# scratch probe scripts
.probe.ts
.schemaprobe.ts
.commitmsg

.next-e2e-degraded
